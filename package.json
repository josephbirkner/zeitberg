{
  "name": "timetracking",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:data": "node scripts/migrate-project-taxonomy.mjs --check",
    "import:todoist": "node scripts/import-todoist.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "npx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

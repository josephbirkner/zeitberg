/**
 * Unbuilt static-checkout fallback. The Pages artifact and local server replace this
 * module with metadata for the exact checkout served, never the latest remote HEAD.
 */
export const BUILD_INFO = { version: "1.3.1", commit: "", dirty: false };
